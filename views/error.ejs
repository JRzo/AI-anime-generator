<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" type="image/x-icon" href="/img/Gemini_Generated_Image_6lsml26lsml26lsm.png">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f8fafc; /* bg-slate-50 */
            color: #1e293b; /* text-slate-800 */
        }
        .container {
            max-width: 90%;
            padding: 2rem;
            text-align: center;
        }
        .error-code {
            font-size: 8rem; /* Equivalent to text-8xl */
            font-weight: 700; /* font-bold */
            color: #ef4444; /* text-red-500 */
            line-height: 1;
            margin-bottom: 1rem;
            animation: bounceIn 1s ease-out;
        }
        .message {
            font-size: 2.25rem; /* text-4xl */
            font-weight: 600; /* font-semibold */
            color: #334155; /* text-slate-700 */
            margin-bottom: 1.5rem;
        }
        .description {
            font-size: 1.125rem; /* text-lg */
            color: #475569; /* text-slate-600 */
            margin-bottom: 2.5rem;
            line-height: 1.6;
        }
        .home-button {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 1.5rem;
            background-color: #3b82f6; /* bg-blue-500 */
            color: #ffffff; /* text-white */
            font-weight: 600; /* font-semibold */
            border-radius: 0.5rem; /* rounded-lg */
            text-decoration: none;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
        }
        .home-button:hover {
            background-color: #2563eb; /* bg-blue-600 */
        }
        .home-button svg {
            margin-right: 0.5rem;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .error-code {
                font-size: 6rem; /* text-6xl for smaller screens */
            }
            .message {
                font-size: 1.75rem; /* text-3xl for smaller screens */
            }
            .description {
                font-size: 1rem; /* text-base for smaller screens */
            }
        }

        /* Simple Bounce Animation for the 404 code */
        @keyframes bounceIn {
            0% {
                opacity: 0;
                transform: scale(0.3) translateY(-50px);
            }
            50% {
                opacity: 1;
                transform: scale(1.1) translateY(10px);
            }
            70% {
                transform: scale(0.9) translateY(-5px);
            }
            100% {
                transform: scale(1) translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container bg-white rounded-xl shadow-lg p-8 md:p-12">
        <div class="error-code">404</div>
        <h1 class="message">Oops! Page Not Found</h1>
        <p class="description">
            We can't seem to find the page you're looking for. It might have been moved or doesn't exist.
            Don't worry, you can always go back to the homepage.
        </p>
        <a href="/" class="home-button">
            <!-- Home icon from Lucide React, using inline SVG -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-home">
                <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
            Go to Homepage
        </a>
    </div>
</body>
</html>
